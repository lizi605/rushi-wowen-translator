# AI 交接文件：《圣经》文体翻译器

> 给接手 AI 的第一条指令：请先完整阅读本文件，再检查仓库代码。不要只替换页面文字，也不要在原项目里加入几个《圣经》关键词就宣告完成。目标是保留已经验证过的技术骨架，重新设计一套适合《圣经》不同文体的产品逻辑、提示词、界面和测试。

## 1. 使用方式

把当前项目复制为一个独立目录和独立 GitHub 仓库，再让 AI 读取本文件并开始工作。

建议给 AI 的开场指令：

```text
请阅读根目录的 AI_HANDOFF_BIBLE_TRANSLATOR.md，接手并完成《圣经》文体翻译器。
先检查当前目录是否仍是「如是我闻」翻译器原项目。如果是原项目，请不要覆盖它，先复制为独立项目再修改。
请按照交接文件实施、测试和本地预览，不要只给方案。
```

建议的新目录：

```text
D:\项目\圣经翻译器
```

建议的新仓库名：

```text
bible-style-translator
```

## 2. 当前项目总结

当前项目名称为 `「如是我闻」翻译器`，是一个把普通中文改写为互联网语境下的佛经体，并能将佛经体解释回现代白话的双向网页工具。

线上网站：

```text
https://fojing-translator.1798859537.workers.dev
```

公开仓库：

```text
https://github.com/lizi605/rushi-wowen-translator
```

正式版本：

```text
https://github.com/lizi605/rushi-wowen-translator/releases/tag/v0.1.0
```

### 已实现能力

- 白话转佛经体。
- 佛经体转现代白话。
- 四种译经语气与四种解经语气。
- 三档输出长度。
- 按故事节点分段讲述和解释。
- 保留原文人物、对象、动作、顺序、关键台词含义与结局。
- 将现代口语改写为相应文体，而非直接照抄原文。
- 图片卡片导出。
- 可复制和下载 Skill 包。
- 桌面端与移动端响应式界面。
- DeepSeek BYOK：每位使用者填写自己的 API Key。
- Cloudflare Workers 公开部署。
- GitHub Release 源码与 Skill 下载。

### 技术栈

- Next.js 16 App Router。
- React 19。
- TypeScript。
- `lucide-react` 图标。
- DeepSeek 的 OpenAI 兼容聊天接口。
- `@opennextjs/cloudflare` 与 Wrangler。
- Node.js 22 用于构建和部署。

### 核心文件

| 文件 | 用途 |
| --- | --- |
| `app/page.tsx` | 主页面、模式选择、API Key 弹窗、请求、复制和图片导出 |
| `app/globals.css` | 全站视觉、响应式布局、弹窗与控件样式 |
| `app/api/translate/route.ts` | 参数校验、限流、模型调用、审校、错误处理和结果收尾 |
| `lib/prompt.ts` | 双向翻译的系统提示词、模式提示词和长度提示词 |
| `lib/cardDownload.ts` | 导出图片文件名逻辑 |
| `scripts/*.test.ts` | 产品规则、提示词、双向功能和下载逻辑测试 |
| `scripts/public-audit.mjs` | 公开发布前的密钥扫描 |
| `wrangler.jsonc` | Cloudflare Worker 配置 |
| `.github/workflows/build-cloudflare-bundle.yml` | 在 Linux 上生成可部署的 OpenNext 产物 |
| `skill-package/speak-fojing/` | 可分发的 AI Skill 源文件 |
| `public/downloads/` | 用户可直接下载的 Skill 文件 |

## 3. 当前请求流程

1. 用户在浏览器中配置自己的 DeepSeek API Key。
2. Key 保存到浏览器 `localStorage`，当前键名为 `rushi-wowen-deepseek-api-key`。
3. 浏览器调用 `/api/translate`，并通过 `Authorization: Bearer <用户Key>` 临时发送 Key。
4. 服务端验证 Key、输入长度、方向、模式和长度档位。
5. 服务端构造提示词并把 Key 临时转发给 DeepSeek。
6. 应用不会把 Key 写入数据库、仓库或服务端环境变量。
7. 未提供 Key 时接口返回 `401`。
8. 无效 Key、额度不足和上游异常分别显示明确错误。

## 4. 本项目最重要的产品经验

### 4.1 格式与道理必须分离

佛经版本只借用了问答、设问、复沓、庄重节奏等形式。每一段道理必须来自用户输入中的具体言语、行为、选择和后果，不能把所有内容强行解释为空性、无住、功德或因果。

《圣经》版本必须继承这条原则：可以借用福音书、箴言、诗篇或书信的文体，但不能把所有输入都强行解释成罪、救赎、恩典、信心、神迹、天意或末日。

### 4.2 原文是例子，不是外层讲述者

佛经版本中，佛陀与须菩提是外层讲法人物，用户原文中的人物只存在于佛陀举出的内层事例中。

《圣经》版本若采用福音书体，可以让耶稣与门徒成为外层讲述者，用户原文人物作为比喻或事例中的人物。绝不能简单把原文人物改名为耶稣和门徒。

这条规则只适用于福音书体。诗篇体、箴言体和书信体不应硬套耶稣与门徒对话。

### 4.3 故事必须完整

- 保留人物和对象关系。
- 保留动作、数量、时间和关键物品。
- 保留关键台词的含义、语气和作用。
- 保留事件顺序、转折、伤害归属和最终结局。
- 原文没有的承诺、动机、反省、旁人反应和后续结果不得补写。
- 读者应能从改写结果还原原文事实。

### 4.4 台词要改写，不要直接引用

现代口语不能整句原样搬入仿古文本。应先拆出其中的身份、对象、否定、条件、请求、威胁、讽刺和情绪，再用目标文体重写。

专名、数字、产品名和无法替换的现代名词可以保留，但句法和叙事口吻需要完成转换。

### 4.5 一个故事可以有多个道理

故事应按真实事件节点自然分段。每一段只解释与该节点最贴切的道理，例如诚信、欺骗、愤怒、暴力、责任、公平、宽恕、边界、合作、尊重、承诺和后果。

不要为追求统一主题而把所有段落解释成同一个教义。

### 4.6 长文本要限制和拆分

当前佛经版本的白话转文体输入上限为 500 个字符，反向解释上限为 3000 个字符。长故事应按情节单元拆分，避免模型为了控制篇幅漏掉结局。

《圣经》版本可以沿用这一策略，并根据测试结果调整。

### 4.7 不得为测试故事写专用产品逻辑

华强卖瓜、宋老虎等材料只能作为测试样本。代码、提示词、页面示例和素材中不得出现只服务于某一个故事的特殊规则。

## 5. 《圣经》版本与佛经版本的关键区别

《圣经》不是一种单一文体，也没有一句适用于所有篇章的统一开头或统一结尾。因此不能把“如是我闻”机械替换成另一句固定文本。

建议把正向转换设计为四种独立文体：

| 推荐模式 | 主要特征 | 使用边界 |
| --- | --- | --- |
| 福音书体 | 场景、门徒发问、耶稣以比喻或事例回答 | 原文人物位于内层事例，不取代耶稣与门徒 |
| 箴言体 | 对偶、警句、行为与后果并列 | 适合短观点和短事件，不强行扩写成长故事 |
| 诗篇体 | 平行句、复沓、情绪与呼告 | 保留原文视角，不凭空增加仇敌、神迹或神意 |
| 书信体 | 劝勉、辨析、条件句和共同体口吻 | 不冒用真实使徒署名，不伪造具体教会和历史出处 |

推荐反向转换继续保留四种现代解释方式：

- 直白释义。
- 耐心讲明。
- 潜台词版。
- 锐评拆穿。

### 推荐的体裁开篇策略

福音书体可以采用场景化开篇，例如：

```text
那时，耶稣同门徒坐在路旁。门徒问他说……
```

开篇中的场景应简短稳定，门徒提出的问题必须与用户输入直接相关。

箴言体通常不需要叙事开篇，应直接进入警句或对偶。

诗篇体可以直接进入第一人称或第三人称的平行抒情，不要默认标注“某人的诗”。

书信体可以用“弟兄们，我愿你们明白……”之类的体裁句式，但不要署名保罗、彼得或其他真实人物，也不要伪造收信教会。

### 推荐的结尾策略

- 福音书体可用门徒或众人的理解、惊讶、沉思作为自然收束。
- 箴言体以最后一组行为与后果的对照收束。
- 诗篇体可用复沓句或情绪回环收束。
- 书信体可用简短劝勉或祝愿收束。
- 不要每篇都机械添加“阿们”。
- 不要生成书名、章号、节号或虚假的经典出处。

## 6. 《圣经》版本提示词的最高规则

接手 AI 应把以下规则写入新的系统提示词和用户提示词：

1. 目标是文体改写，不是真实经文翻译，也不声称内容来自《圣经》。
2. 文体来源和道理来源必须分开。文体可以参考《圣经》，判断必须来自输入事实。
3. 保留原文事实闭集，不增加人物、动机、规则、承诺、反省、神迹、预言或后续结果。
4. 严格沿用原文事件顺序并讲到结局。
5. 原文人物在福音书体中只能成为耶稣所举事例中的人物。
6. 现代口语应改写为目标文体，不能连续照抄。
7. 每段解释只讨论该段明确支持的伦理或现实问题。
8. 不得默认套用罪、救赎、恩典、信心、审判、天意、撒但或末日。
9. 只有输入明确涉及相应问题时，才可适量使用宗教性概念。
10. 中性事务优先解释沟通、责任、方法、风险、分寸和合作。
11. 不美化欺骗、暴力、胁迫和伤害，也不把受害等同于道德缺陷。
12. 不用宗教语言消解真假、证据、数量、损失、伤害与责任。
13. 不伪造书名、章节、经文编号、使徒署名或历史出处。
14. 输出只包含改写正文，不解释写作过程，不使用 Markdown 标题。
15. 长故事按事件节点自然分段，短输入不为凑篇幅编故事。

## 7. 品牌与视觉需要重新决定

不要沿用佛像、菩提树、须菩提、佛字图标、佛经卷轴和现有 Skill 名称。

开始设计前，建议让用户确认以下三项：

1. 产品名称。
2. 主视觉风格。
3. 默认正向文体。

可供选择的名称方向：

- `「太初有道」翻译器`
- `「经上记着」翻译器`
- `《圣经》文体翻译器`

名称只是产品品牌，不得暗示输出是真实经文。

可供选择的视觉方向：

- 古典油画与历史场景，但人物不过度神化。
- 木刻版画与旧书排版。
- 羊皮纸质感、古铜金、深橄榄绿与暗红点缀。

避免直接复用佛经版图片。新图应表现目标文体的真实场景，例如教师与门徒在湖边、山坡或室内交谈。不要给人物加入夸张神光、漂浮光环或奇幻特效，除非用户明确要求。

## 8. 代码改造清单

### 必须保留

- Next.js 双向翻译架构。
- `/api/translate` 服务端代理。
- BYOK API Key 弹窗。
- `Authorization: Bearer <用户Key>` 请求方式。
- 本地存储、显示/隐藏、保存和清除 Key。
- 输入长度限制。
- 限流与错误处理。
- 图片导出。
- 响应式布局。
- 公开发布密钥审计。
- Cloudflare 配置和 Linux 构建工作流。

### 必须修改

- 产品名称、标题、说明、按钮文字和示例。
- `API_KEY_STORAGE_KEY`，建议改为 `bible-style-deepseek-api-key`。
- `lib/prompt.ts` 中全部佛经提示词、类型名和模式定义。
- `route.ts` 中固定佛经开头、固定结尾、佛经收尾检查和佛教专用回退文案。
- 页面中的“译经、解经、高僧、译僧、须菩提”等词。
- 图片导出标题、页脚和文件名。
- 图片、图标、下载 Skill、视频脚本和素材署名。
- README、项目描述、仓库链接和原作致谢说明。
- Wrangler Worker 名称，建议改为 `bible-style-translator`。
- GitHub Actions 中与仓库名、图表名有关的配置。

### 建议重命名的类型和函数

```text
ZhouliMode       -> ScriptureMode
ZhouliLevel      -> ScriptureLevel
ZhouliDirection  -> ScriptureDirection
buildUserPrompt  -> buildScripturePrompt
SYSTEM_PROMPT    -> SCRIPTURE_SYSTEM_PROMPT
to_zhouli        -> to_scripture
```

不要为了省事长期保留 `zhouli`、`fojing` 等旧命名。短期兼容可以存在，但交付前应完成清理。

### 全仓搜索清理

改造后至少运行：

```bash
rg -n "佛|须菩提|如是我闻|佛经|译僧|高僧|因果|功德|fojing|zhouli|speak-fojing" . \
  -g "!node_modules" -g "!.next" -g "!.open-next" -g "!.git"
```

逐项判断哪些是历史致谢，哪些是必须删除的产品残留。不要直接全局替换，因为不同文体没有一一对应词汇。

## 9. 安全与 API Key 规则

- 新项目继续使用 BYOK，不提供共享 DeepSeek API Key。
- 不读取 `process.env.DEEPSEEK_API_KEY` 作为公共回退。
- 不提供无 Key 的伪模型演示结果。
- 不在日志中打印 `Authorization` 请求头。
- 不把真实 Key 写入 `.env.example`、README、测试样本或截图。
- `.env.local`、`.dev.vars`、Cloudflare Token 和 GitHub Token 不得提交。
- 共享电脑使用完毕后，用户应能一键清除本机 Key。
- 线上服务只通过 HTTPS 使用。
- 发布前必须运行 `npm run public:audit`。

## 10. 测试样本与验收标准

不要只用一个网络梗测试。至少覆盖以下输入类型：

- 一句很短的请求或观点。
- 多人物、有转折、有明确结局的完整故事。
- 包含欺骗、愤怒或暴力的冲突故事。
- 没有明显善恶冲突的中性日常事务。
- 第一人称情绪或求助文本。
- 第三方影视台词或互联网素材。
- 包含现代产品名、数字和专名的文本。
- 文体文本反向翻回现代白话。

正向转换验收：

- 四种文体输出有明显差异。
- 福音书体的外层人物与内层故事人物没有混淆。
- 箴言体没有被硬扩写成对话故事。
- 诗篇体保留原文视角和情绪。
- 书信体没有伪造真实使徒与教会。
- 道理来自输入，不是万能宗教答案。
- 故事事实、顺序和结局完整。
- 现代台词已完成文体化改写。
- 没有虚假书名、章号、节号和出处。

反向转换验收：

- 第一人称仍保持第一人称。
- 不以“这段话的意思是”之类报告腔开头。
- 不增加原文没有的动机和评价。
- 短文本可以得到短而准确的答案。

功能验收：

- 未配置 Key 时自动打开配置弹窗。
- 保存 Key 后刷新页面仍可读取。
- 清除 Key 后无法继续调用。
- 错误 Key 返回明确提示。
- 桌面端和移动端无文字遮挡、溢出或控件错位。
- 图片导出标题和文件名使用新品牌。
- GitHub 下载、Skill 下载和原作致谢链接正确。

## 11. 开发与发布命令

本地检查：

```bash
npm ci
npm run public:audit
npm test
npm run typecheck
npm run build
npm run dev
```

本地页面通常为：

```text
http://127.0.0.1:3000
```

## 12. Cloudflare 部署经验

本项目曾在 Windows 上直接运行 OpenNext 构建，上传后出现以下线上错误：

```text
ChunkLoadError: Failed to load chunk server/chunks/...
```

原因是 OpenNext 在 Windows 上生成的服务器分块路径不可靠。不要再次直接使用 Windows 产物作为最终部署包。

仓库已经提供：

```text
.github/workflows/build-cloudflare-bundle.yml
```

推荐部署流程：

1. 把代码推送到 GitHub。
2. 手动触发 `Build Cloudflare bundle` 工作流。
3. 等待 Ubuntu 构建成功。
4. 下载名为 `cloudflare-bundle` 的 Artifact。
5. 用 Artifact 内容覆盖本地 `.open-next`。
6. 只运行 OpenNext 的 deploy 阶段上传 Linux 产物。
7. 检查主页返回 `200`。
8. 检查无 Key 调用 `/api/translate` 返回 `401`。

GitHub CLI 示例：

```bash
gh workflow run build-cloudflare-bundle.yml --ref main
gh run list --workflow build-cloudflare-bundle.yml --limit 1
gh run watch <RUN_ID> --exit-status
gh run download <RUN_ID> --name cloudflare-bundle --dir <TEMP_DIR>
```

部署前还需要：

- `wrangler login` 已授权。
- Cloudflare 账号已登记免费的 `workers.dev` 子域名。
- `wrangler.jsonc` 中 `workers_dev` 为 `true`。
- 不需要购买域名。

## 13. 接手 AI 的执行顺序

1. 确认没有在原佛经项目上直接覆盖。
2. 阅读 README、`lib/prompt.ts`、API route、页面和测试。
3. 让用户确认名称、视觉方向和默认文体；用户不回答时采用保守默认值。
4. 先重构类型与提示词，再处理页面文案和视觉。
5. 清除所有佛经专用固定开头、结尾和回退逻辑。
6. 为四种《圣经》文体分别实现结构约束和结果检查。
7. 更换全部视觉素材和 Skill 包。
8. 增加多类型测试，不得只验证一个网络故事。
9. 运行审计、测试、类型检查和生产构建。
10. 启动本地服务器，检查桌面端与移动端。
11. 推送新的独立 GitHub 仓库。
12. 使用 Linux Artifact 部署 Cloudflare。
13. 验证线上主页、API Key 配置、无 Key 错误和一次真实生成。
14. 把最终网址写入 README 和 GitHub About。

## 14. 完成定义

只有同时满足以下条件，才能认为《圣经》文体翻译器已经完成：

- 它是独立项目，不覆盖「如是我闻」翻译器。
- 页面、提示词、素材、Skill 和测试都已完成新主题改造。
- 四种文体不是同一输出换几个关键词。
- 不把所有输入强行解释为固定宗教教义。
- 原文事实、顺序、台词含义和结局可逆。
- 使用者必须配置自己的 API Key。
- 没有真实密钥进入仓库和部署环境。
- 测试、类型检查、生产构建和公开审计通过。
- 线上页面可公开访问，主页返回 `200`。
- 无 Key 的线上 API 返回 `401`。
- README、GitHub 仓库和下载文件都指向新项目。

## 15. 最后提醒

这个新产品的核心不是“给现代文本加几个上帝、门徒、罪与恩典的词”，而是把原文事实放进恰当的《圣经》文学结构中，同时让伦理判断严格服从输入内容。

形式可以古老，事实不能走样；语言可以庄重，道理不能硬套。
